; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32-s3-n16r8]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
monitor_speed = 115200
upload_port = COM6
upload_speed = 460800

; 串口监视器设置
monitor_rts = 0
monitor_dtr = 0
monitor_raw = yes
monitor_echo = yes

upload_flags =
    --before=default_reset
    --after=hard_reset

build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -D BOARD_HAS_PSRAM
    -mfix-esp32-psram-cache-issue
    -D CONFIG_SPIRAM_USE_CAPS_ALLOC=1
    -D CONFIG_SPIRAM_USE_MALLOC=1
    -std=gnu++17
    -fexceptions
    -D MAIN_APPLICATION_ENABLED=1
    -DCORE_DEBUG_LEVEL=1          ; 降低到1，大幅减少调试开销
    -DDEBUG_ESP_SSL=0             ; 禁用SSL调试日志
    -DDEBUG_ESP_WIFI=0            ; 禁用WiFi调试日志
    -DDEBUG_ESP_TLS_MEM=0         ; 禁用TLS内存调试
    -DCONFIG_MBEDTLS_CERTIFICATE_BUNDLE=n
    -DCONFIG_MBEDTLS_SSL_MAX_CONTENT_LEN=512
    -DCONFIG_MBEDTLS_TLS_MEMORY_DEBUG=0
    -DCONFIG_MBEDTLS_INTERNAL_MEM_SIZE=163840  ; 尝试160KB，尽管Arduino框架可能忽略

board_build.arduino.memory_type = qio_opi
board_build.sdkconfig = sdkconfig.defaults
board_upload.flash_size = 16MB

; 限制并行编译作业以减少内存使用
build_jobs = 1

; 依赖库
lib_deps =
    bodmer/TFT_eSPI@^2.5.43
    bblanchon/ArduinoJson@^6.21.3
    # pschatzmann/ESP32-A2DP@^1.7.7  # 暂时注释，待解决
    me-no-dev/ESPAsyncWebServer@^1.2.4
    Links2004/WebSockets@^2.6.1
    tzapu/WiFiManager@^2.0.17

; 构建配置
build_unflags = -std=gnu++11

; 源文件过滤：排除测试文件
build_src_filter =
    +<*>
    -<src/test_pins.cpp>
    -<src/test_hardware_integration.cpp>

; SPIFFS文件系统配置
board_build.filesystem = spiffs


